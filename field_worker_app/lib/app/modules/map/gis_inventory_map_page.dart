import 'package:flutter/material.dart';

class GisInventoryMapPage extends StatelessWidget {
  const GisInventoryMapPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('GIS Inventory Map'),
      ),
      body: const Center(
        child: Text('GIS Inventory Map Screen'),
      ),
    );
  }
}
